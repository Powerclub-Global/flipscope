# Local dev values come from `npx supabase start` output.
# Staging/production values live in the deploy platform's secret store — never in git.
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=replace-with-anon-key-from-supabase-start
